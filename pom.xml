<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.gomoku</groupId>
    <artifactId>my-project</artifactId>
    <version>1.0-SNAPSHOT</version>
    <dependencies>
        <!-- 添加项目依赖 -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <!-- 配置构建选项 -->
        <plugins>

        </plugins>
    </build>
</project>